import '../Text.css';

function SignIn(props) {
	return (
		<div>
			<h2 className="blueText" style={{fontSize: "70px"}}> {props.title} </h2>
			<h2 className="yellowText" style={{fontSize: "70px"}} > {props.title}</h2>
			<h2 className="pinkText" style={{fontSize: "70px"}}> {props.title}</h2>
		</div>
	);
}

export default SignIn
